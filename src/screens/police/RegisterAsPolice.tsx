import React from "react";
import { View, Text } from "react-native";
import { NavigationProps } from "@types";

export function RegisterAsPolice({ navigation }: NavigationProps<"Getstarted">) {
    return (
        <View>
            <Text></Text>
        </View>
    );
}
